// Central place for the things you'll personalize before publishing.
// Nothing here is secret — these values ship to the browser.

export const site = {
  // Your name / brand shown in the footer and CTA.
  freelancerName: "Amar Gupta",

  // The product name shown across the site.
  productName: "Invoice → Spreadsheet",

  // One-line value prop used in the hero + page <title>.
  tagline: "Turn messy invoice PDFs into a clean spreadsheet in seconds.",

  // Public canonical URL (used for metadata). Update after you attach a domain.
  baseUrl: "https://example.com",
};
